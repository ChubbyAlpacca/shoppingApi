CREATE TABLE PRODUCTS
(
    PRODUCT_ID  NUMBER(19, 0) NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    DESCRIPTION VARCHAR(255),
    CATEGORY    VARCHAR(255),
    PRICE        DOUBLE PRECISION,
    PRIMARY KEY (PRODUCT_ID)
)